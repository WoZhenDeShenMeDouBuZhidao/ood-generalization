# Dev Log: 2026-04-02

- 使用者要求把 synthetic 核心程式整合回共用核心
- 刪除：
  - `src/main_synthetic.py`
  - `src/trainer_synthetic.py`
- 將 synthetic loader 接入 `src/main.py`
- 將 `src/trainer.py` 改為：
  - 可設定 `MAX_EPOCHS`
  - 可選擇是否對 test sets 畫 SHAP
- `run_synthetic_ood.py` 改成與 `run_acsincome.py` 相同風格
- 刪除自動輸出的 `benchmark_results.json`

## Current State After Integration

- synthetic 與 ACS 已共用同一套訓練核心
- synthetic 仍可畫：
  - training curves
  - validation SHAP
  - OOD test SHAP
- 下一步若要繼續推進，優先方向應該是：
  - 改善 ACS feature ranking / weight assignment
  - 而不是回頭重寫 `FeatureGradCELoss`
