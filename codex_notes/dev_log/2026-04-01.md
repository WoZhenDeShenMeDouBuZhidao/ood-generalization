# Dev Log: 2026-04-01

- 讀取 `prompt.md`，確認任務是用 synthetic dataset 驗證 `FeatureGradCELoss`
- 建立 `synthetic_ood/dataset.py`
- 當時先做了一套獨立 synthetic pipeline，快速驗證 loss 是否有效
- 實驗結果顯示：
  - `Favor causal` 比 baseline 有更高 OOD
  - `Favor spurious` 比 baseline 有更低 OOD
- 記錄 synthetic benchmark 摘要

### Initial FeatureGradCELoss Sanity Benchmark

Train/validation 使用穩定 causal features 與高度相關 spurious feature；OOD test 將 `spurious_main` 的相關性從 `+0.95` 翻成 `-0.95`。

Feature weight ranking 搭配 `feature_grad_ce`:

- `Favor spurious`: `spurious_main > noise > causal_main > causal_aux`
- `Baseline`: all equal rank, `reg_scale = 0` 等同 CE loss
- `Favor causal`: `causal_main > causal_aux > noise > spurious_main`
- `Optimal`: remove `spurious_main`, `noise`, all equal rank

| Name | ID | OOD MEAN | OOD WORST |
| ---- | --: | --------: | ---------: |
| `Favor spurious` | 0.9691 | 0.6390 | 0.6390 |
| `Baseline` | 0.9693 | 0.6828 | 0.6828 |
| `Favor causal (reg=1)` | 0.9694 | 0.7090 | 0.7090 |
| `Optimal` | 0.9214 | 0.9323 | 0.9323 |

結論：

- 初版 `FeatureGradCELoss` 的方向性成立。
- favor causal 會提升 OOD；favor spurious 會降低 OOD。
- `Optimal` 顯示只要移除 spurious/noise，synthetic OOD upper bound 很高，因此後續 loss 設計值得繼續推進。
