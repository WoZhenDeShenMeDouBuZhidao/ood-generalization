# Dev Log: 2026-05-11

Cleanup note, 2026-05-31:

- Raw metric benchmark logs were deleted after the detailed Accuracy/F1 results below
  were recorded.
- Raw metric benchmark JSON and partial JSON files under `acs_tasks/metric_benchmarks/`
  were also deleted; the README tables and this dev log keep the retained results.

## ACS Accuracy/F1 Metric Tables

Goal:

- Finish ACS `CrossEntropyLoss fix dataset issue` metric reruns after expanding metrics to
  Accuracy/F1 with `ID`, `OOD MEAN`, `OOD WORST`, and `OOD STD`.
- Ensure benchmark processes run on GPU and record device metadata.

Implementation:

- Updated `run_acs_metric_benchmark.py` (now archived at
  `archive/scripts/run_acs_metric_benchmark.py`):
  - supports `--setting cross_entropy_original` and
    `--setting cross_entropy_fix_dataset_issue`.
  - records `device` in output JSON.
  - fails fast if `--device cuda:*` is requested but CUDA is not visible.
- Updated `aggregate_acs_metric_partials.py` (now archived at
  `archive/scripts/aggregate_acs_metric_partials.py`) to preserve a `devices` list when
  aggregating single-seed partial jobs across GPUs.
- Restored `BatchNorm1d` in `src/mlp.py` for the fix-dataset-issue rerun.
- Updated `README.md` Dataset Conclusions:
  - removed the old ACC-only HTML table.
  - added grouped Accuracy and F1 tables with both `CrossEntropyLoss original setting`
    and `CrossEntropyLoss fix dataset issue`.

Verification:

- Confirmed PyTorch sees two GPUs:
  - `cuda:0`: NVIDIA GeForce RTX 5090
  - `cuda:1`: NVIDIA GeForce RTX 5090
- Completed all 8 ACS fix-dataset-issue results.
- Direct dataset jobs recorded `device: cuda:0` or `device: cuda:1`.
- Partial jobs for `acspubliccoverage` and `acstraveltime` were aggregated from
  single-seed JSON files and record `devices: ["cuda:0", "cuda:1"]`.
- Confirmed no `run_acs_metric_benchmark.py` process remained after completion.

Synthetic update:

- Added `synthetic_ood` `CrossEntropyLoss original setting` to the new metric table.
- Ran on `cuda:0` with the same controlled synthetic config and seeds as the
  `FeatureImportanceTargetCELoss` synthetic benchmark.
- Result:
  - Accuracy: `ID = 0.9693`, `OOD MEAN = 0.6828`, `OOD WORST = 0.6828`,
    `OOD STD = 0.0000`
  - F1: `ID = 0.9686`, `OOD MEAN = 0.6759`, `OOD WORST = 0.6759`,
    `OOD STD = 0.0000`
- Did not add a `fix dataset issue` synthetic entry because the dataset is controlled and
  does not need dataset-specific fixes.
