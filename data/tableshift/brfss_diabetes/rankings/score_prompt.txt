# tableshift/brfss_diabetes score prompts

## 1. feature_id=0 feature=PHYSHLTH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 0,
  "feature": "PHYSHLTH",
  "type": "continuous",
  "description": "Number of days during the past 30 days where physical health was not good",
  "note": "Values: 1 - 30 Number of days, 88 None, 77 Don’t know/Not sure, 99 Refused, BLANK Not asked or Missing"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 0,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 2. feature_id=1 feature=HIGH_BLOOD_PRESS
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 1,
  "feature": "HIGH_BLOOD_PRESS",
  "type": "categorical",
  "description": "(Ever told) you have high blood pressure",
  "values": "1.0=No; 2.0=Yes; 9.0=Don’t know/Not Sure/Refused/Missing"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 1,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 3. feature_id=2 feature=CHOL_CHK_PAST_5_YEARS
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 2,
  "feature": "CHOL_CHK_PAST_5_YEARS",
  "type": "categorical",
  "description": "Time since last blooc cholesterol check",
  "values": "1.0=Never; 2.0=Within the past year (anytime less than 12 months ago); 3.0=Within the past 2 years (more than 1 year but less than 2 years ago); 4.0=Within the past 5 years (more than 2 years but less than 5 years ago); 5.0=5 or more years ago; 7.0=Don’t kn...",
  "note": "Aligned version of 'CHOLCHK*' features from 2015-2021; see _align_chol_chk() below.."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 2,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 4. feature_id=3 feature=TOLDHI
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 3,
  "feature": "TOLDHI",
  "type": "categorical",
  "description": "Ever been told you have high blood cholesterol",
  "values": "1.0=Yes; 2.0=No; 7.0=Don’t know/Not Sure; 9.0=Refused"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 3,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 5. feature_id=4 feature=BMI5
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 4,
  "feature": "BMI5",
  "type": "continuous",
  "description": "Body Mass Index (BMI)",
  "note": "Values: 1 - 9999 1 or greater - Notes: WTKG3/(HTM4*HTM4) (Has 2 implied decimal places); BLANK: Don’t know/Refused/Missing."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 4,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 6. feature_id=5 feature=BMI5CAT
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 5,
  "feature": "BMI5CAT",
  "type": "categorical",
  "description": "Body Mass Index (BMI) category",
  "values": "1.0=Underweight (BMI < 1850); 2.0=Normal Weight (1850 <= BMI < 2500); 3.0=Overweight (2500 <= BMI < 3000); 4.0=Obese (3000 <= BMI < 9999)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 5,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 7. feature_id=6 feature=SMOKE100
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 6,
  "feature": "SMOKE100",
  "type": "boolean_categorical",
  "description": "Answer to the question 'Have you smoked at least 100 cigarettes in your entire life?'",
  "values": "1.0=Yes; 2.0=No"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 6,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 8. feature_id=7 feature=SMOKDAY2
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 7,
  "feature": "SMOKDAY2",
  "type": "categorical",
  "description": "Answer to the question 'Do you now smoke cigarettes every day, some days, or not at all?'",
  "values": "1.0=Every day; 2.0=Some days; 3.0=Not at all; 7.0=Don´t Know/Not Sure; 9.0=Refused"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 7,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 9. feature_id=8 feature=CVDSTRK3
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 8,
  "feature": "CVDSTRK3",
  "type": "categorical",
  "description": "Ever had a stroke, or been told you had a stroke",
  "values": "1.0=Yes; 2.0=No; 7.0=Don’t know/Not Sure; 9.0=Refused"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 8,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 10. feature_id=9 feature=MICHD
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 9,
  "feature": "MICHD",
  "type": "categorical",
  "description": "Reports of coronary heart disease (CHD) or myocardial infarction (MI)",
  "values": "1.0=Reported having myocardial infarction or coronary heart disease; 2.0=Did not report having myocardial infarction or coronary heart disease"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 9,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 11. feature_id=10 feature=FRUIT_ONCE_PER_DAY
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 10,
  "feature": "FRUIT_ONCE_PER_DAY",
  "type": "categorical",
  "description": "Fruit consumption",
  "values": "1.0=Consumed fruit one or more times per day; 2.0=Consumed fruit less than one time per day; 9.0=Don't know, refused or missing values"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 10,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 12. feature_id=11 feature=VEG_ONCE_PER_DAY
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 11,
  "feature": "VEG_ONCE_PER_DAY",
  "type": "categorical",
  "description": "Vegetable consumption",
  "values": "1.0=Consumed vegetables one or more times per day; 2.0=Consumed vegetables less than one time per day; 9.0=Don't know, refused or missing values"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 11,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 13. feature_id=12 feature=DRNK_PER_WEEK
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 12,
  "feature": "DRNK_PER_WEEK",
  "type": "continuous",
  "description": "Total number of alcoholic beverages consumed per week"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 12,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 14. feature_id=13 feature=RFBING5
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 13,
  "feature": "RFBING5",
  "type": "categorical",
  "description": "Respondent is binge drinker",
  "values": "1.0=No; 2.0=Yes; 9.0=Don't know/Refused/Missing"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 13,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 15. feature_id=14 feature=TOTINDA
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 14,
  "feature": "TOTINDA",
  "type": "categorical",
  "description": "Physical activity or exercise during the past 30 days other than their regular job",
  "values": "1.0=Had physical activity or exercise in last 30 days; 2.0=No physical activity or exercise in last 30 days"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 14,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 16. feature_id=15 feature=INCOME
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 15,
  "feature": "INCOME",
  "type": "categorical",
  "description": "Annual household income from all sources",
  "values": "1.0=Less than $10,000; 2.0=Less than $15,000 ($10,000 to less than $15,000); 3.0=Less than $20,000 ($15,000 to less than $20,000); 4.0=Less than $25,000 ($20,000 to less than $25,000); 5.0=Less than $35,000 ($25,000 to less than $35,000); 6.0=Less than $50,..."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 15,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 17. feature_id=16 feature=MARITAL
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 16,
  "feature": "MARITAL",
  "type": "categorical",
  "description": "Marital status",
  "values": "1.0=Married; 2.0=Divorced; 3.0=Widowed; 4.0=Separated; 5.0=Never married; 6.0=A member of an unmarried couple; 9.0=Refused"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 16,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 18. feature_id=17 feature=CHECKUP1
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 17,
  "feature": "CHECKUP1",
  "type": "categorical",
  "description": "Time since last visit to the doctor for a checkup",
  "values": "1.0=Within past year (anytime < 12 months ago); 2.0=Within past 2 years (1 year but < 2 years ago); 3.0=Within past 5 years (2 years but < 5 years ago); 4.0=5 or more years ago; 7.0=Don’t know/Not sure; 8.0=Never; 9.0=Refuse",
  "note": "Question: About how long has it been since you last visited a doctor for a routine checkup? [A routine checkup is a general physical exam, not an exam for a specific injury, illness, or condition.]"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 17,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 19. feature_id=18 feature=EDUCA
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 18,
  "feature": "EDUCA",
  "type": "categorical",
  "description": "Highest grade or year of school completed",
  "values": "1.0=Never attended school or only kindergarten; 2.0=Grades 1 through 8 (Elementary); 3.0=Grades 9 through 11 (Some high school); 4.0=Grade 12 or GED (High school graduate); 5.0=College 1 year to 3 years (Some college or technical school); 6.0=College 4 year...",
  "note": "Question: What is the highest grade or year of school you completed?"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 18,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 20. feature_id=19 feature=HEALTH_COV
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 19,
  "feature": "HEALTH_COV",
  "type": "categorical",
  "description": "Current health care coverage",
  "values": "1.0=Have health care coverage; 2.0=Do not have health care coverage; 9.0=Not aged 18-64, Don’t know/Not Sure, Refused or Missing"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 19,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 21. feature_id=20 feature=MENTHLTH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 20,
  "feature": "MENTHLTH",
  "type": "continuous",
  "description": "Answer to the question 'for how many days during the past 30 days was your mental health not good?'",
  "note": "Values: 1 - 30: Number of days, 88: None, 77: Don’t know/Not sure, 99: Refused."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 20,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 22. feature_id=21 feature=IYEAR
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 21,
  "feature": "IYEAR",
  "type": "continuous",
  "description": "Survey year"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 21,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 23. feature_id=22 feature=STATE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 22,
  "feature": "STATE",
  "type": "categorical",
  "description": "State",
  "values": "1.0=Alabama; 4.0=Arizona; 5.0=Arkansas; 6.0=California; 8.0=Colorado; 9.0=Connecticut; 10.0=Delaware; 11.0=District of Columbia; 12.0=Florida; 13.0=Georgia; 15.0=Hawaii; 16.0=Idaho; ... (+40 more)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 22,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 24. feature_id=23 feature=MEDCOST
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 23,
  "feature": "MEDCOST",
  "type": "categorical",
  "description": "Answer to the question 'Was there a time in the past 12 months when you needed to see a doctor but could not because of cost?'",
  "values": "1.0=Yes; 2.0=No; 7.0=Don't know/not sure; 9.0=Refused"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 23,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 25. feature_id=24 feature=SEX
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `brfss_diabetes`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 24,
  "feature": "SEX",
  "type": "categorical",
  "description": "Sex of respondent",
  "values": "1=Male; 2=Female"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 24,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.
