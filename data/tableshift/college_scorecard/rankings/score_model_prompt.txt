# tableshift/college_scorecard score prompts

## 1. feature_id=0 feature=ACTCMMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 0,
  "feature": "ACTCMMID",
  "type": "continuous",
  "description": "Midpoint of the ACT cumulative score"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 0,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 2. feature_id=1 feature=ACTENMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 1,
  "feature": "ACTENMID",
  "type": "continuous",
  "description": "Midpoint of the ACT English score"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 1,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 3. feature_id=2 feature=ACTMTMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 2,
  "feature": "ACTMTMID",
  "type": "continuous",
  "description": "Midpoint of the ACT math score"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 2,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 4. feature_id=3 feature=ACTWRMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 3,
  "feature": "ACTWRMID",
  "type": "continuous",
  "description": "Midpoint of the ACT writing score"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 3,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 5. feature_id=4 feature=ADM_RATE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 4,
  "feature": "ADM_RATE",
  "type": "continuous",
  "description": "Admission rate"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 4,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 6. feature_id=5 feature=ADM_RATE_ALL
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 5,
  "feature": "ADM_RATE_ALL",
  "type": "continuous",
  "description": "Admission rate for all campuses rolled up to the 6-digit OPE ID"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 5,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 7. feature_id=6 feature=AVGFACSAL
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 6,
  "feature": "AVGFACSAL",
  "type": "continuous",
  "description": "Average faculty salary"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 6,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 8. feature_id=7 feature=AccredAgency
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 7,
  "feature": "AccredAgency",
  "type": "categorical",
  "description": "Accreditor for institution"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 7,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 9. feature_id=8 feature=CCSIZSET
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 8,
  "feature": "CCSIZSET",
  "type": "categorical",
  "description": "Carnegie Classification -- size and setting"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 8,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 10. feature_id=9 feature=CONTROL
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 9,
  "feature": "CONTROL",
  "type": "categorical",
  "description": "Control of institution"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 9,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 11. feature_id=10 feature=COSTT4_A
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 10,
  "feature": "COSTT4_A",
  "type": "continuous",
  "description": "Average cost of attendance (academic year institutions)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 10,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 12. feature_id=11 feature=COSTT4_P
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 11,
  "feature": "COSTT4_P",
  "type": "continuous",
  "description": "Average cost of attendance (program-year institutions)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 11,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 13. feature_id=12 feature=DISTANCEONLY
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 12,
  "feature": "DISTANCEONLY",
  "type": "categorical",
  "description": "Flag for distance-education-only education"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 12,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 14. feature_id=13 feature=HBCU
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 13,
  "feature": "HBCU",
  "type": "categorical",
  "description": "Flag for Historically Black College and University"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 13,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 15. feature_id=14 feature=HIGHDEG
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 14,
  "feature": "HIGHDEG",
  "type": "categorical",
  "description": "Highest degree awarded",
  "values": "0=Non-degree-granting; 1=Certificate degree; 2=Associate degree; 3=Bachelor's degree; 4=Graduate degree"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 14,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 16. feature_id=15 feature=INEXPFTE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 15,
  "feature": "INEXPFTE",
  "type": "continuous",
  "description": "Instructional expenditures per full-time equivalent student"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 15,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 17. feature_id=16 feature=LOCALE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 16,
  "feature": "LOCALE",
  "type": "categorical",
  "description": "Locale of institution"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 16,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 18. feature_id=17 feature=NPT4_PROG
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 17,
  "feature": "NPT4_PROG",
  "type": "continuous",
  "description": "Average net price for the largest program at the institution for program-year institutions"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 17,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 19. feature_id=18 feature=NUMBRANCH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 18,
  "feature": "NUMBRANCH",
  "type": "integer",
  "description": "Number of branch campuses"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 18,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 20. feature_id=19 feature=PCIP01
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 19,
  "feature": "PCIP01",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Agriculture, Agriculture Operations, And Related Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 19,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 21. feature_id=20 feature=PCIP03
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 20,
  "feature": "PCIP03",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Natural Resources And Conservation."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 20,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 22. feature_id=21 feature=PCIP04
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 21,
  "feature": "PCIP04",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Architecture And Related Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 21,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 23. feature_id=22 feature=PCIP05
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 22,
  "feature": "PCIP05",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Area, Ethnic, Cultural, Gender, And Group Studies."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 22,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 24. feature_id=23 feature=PCIP09
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 23,
  "feature": "PCIP09",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Communication, Journalism, And Related Programs."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 23,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 25. feature_id=24 feature=PCIP10
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 24,
  "feature": "PCIP10",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Communications Technologies/Technicians And Support Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 24,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 26. feature_id=25 feature=PCIP11
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 25,
  "feature": "PCIP11",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Computer And Information Sciences And Support Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 25,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 27. feature_id=26 feature=PCIP12
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 26,
  "feature": "PCIP12",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Personal And Culinary Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 26,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 28. feature_id=27 feature=PCIP13
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 27,
  "feature": "PCIP13",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Education."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 27,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 29. feature_id=28 feature=PCIP14
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 28,
  "feature": "PCIP14",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Engineering."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 28,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 30. feature_id=29 feature=PCIP15
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 29,
  "feature": "PCIP15",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Engineering Technologies And Engineering-Related Fields."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 29,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 31. feature_id=30 feature=PCIP16
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 30,
  "feature": "PCIP16",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Foreign Languages, Literatures, And Linguistics."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 30,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 32. feature_id=31 feature=PCIP19
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 31,
  "feature": "PCIP19",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Family And Consumer Sciences/Human Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 31,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 33. feature_id=32 feature=PCIP22
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 32,
  "feature": "PCIP22",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Legal Professions And Studies."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 32,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 34. feature_id=33 feature=PCIP23
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 33,
  "feature": "PCIP23",
  "type": "continuous",
  "description": "Percentage of degrees awarded in English Language And Literature/Letters."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 33,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 35. feature_id=34 feature=PCIP24
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 34,
  "feature": "PCIP24",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Liberal Arts And Sciences, General Studies And Humanities."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 34,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 36. feature_id=35 feature=PCIP25
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 35,
  "feature": "PCIP25",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Library Science."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 35,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 37. feature_id=36 feature=PCIP26
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 36,
  "feature": "PCIP26",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Biological And Biomedical Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 36,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 38. feature_id=37 feature=PCIP27
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 37,
  "feature": "PCIP27",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Mathematics And Statistics."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 37,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 39. feature_id=38 feature=PCIP29
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 38,
  "feature": "PCIP29",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Military Technologies And Applied Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 38,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 40. feature_id=39 feature=PCIP30
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 39,
  "feature": "PCIP30",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Multi/Interdisciplinary Studies."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 39,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 41. feature_id=40 feature=PCIP31
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 40,
  "feature": "PCIP31",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Parks, Recreation, Leisure, And Fitness Studies."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 40,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 42. feature_id=41 feature=PCIP38
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 41,
  "feature": "PCIP38",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Philosophy And Religious Studies."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 41,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 43. feature_id=42 feature=PCIP39
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 42,
  "feature": "PCIP39",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Theology And Religious Vocations."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 42,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 44. feature_id=43 feature=PCIP40
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 43,
  "feature": "PCIP40",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Physical Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 43,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 45. feature_id=44 feature=PCIP41
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 44,
  "feature": "PCIP41",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Science Technologies/Technicians."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 44,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 46. feature_id=45 feature=PCIP42
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 45,
  "feature": "PCIP42",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Psychology."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 45,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 47. feature_id=46 feature=PCIP43
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 46,
  "feature": "PCIP43",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Homeland Security, Law Enforcement, Firefighting And Related Protective Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 46,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 48. feature_id=47 feature=PCIP44
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 47,
  "feature": "PCIP44",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Public Administration And Social Service Professions."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 47,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 49. feature_id=48 feature=PCIP45
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 48,
  "feature": "PCIP45",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Social Sciences."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 48,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 50. feature_id=49 feature=PCIP46
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 49,
  "feature": "PCIP46",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Construction Trades."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 49,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 51. feature_id=50 feature=PCIP47
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 50,
  "feature": "PCIP47",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Mechanic And Repair Technologies/Technicians."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 50,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 52. feature_id=51 feature=PCIP48
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 51,
  "feature": "PCIP48",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Precision Production."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 51,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 53. feature_id=52 feature=PCIP49
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 52,
  "feature": "PCIP49",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Transportation And Materials Moving."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 52,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 54. feature_id=53 feature=PCIP50
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 53,
  "feature": "PCIP50",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Visual And Performing Arts."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 53,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 55. feature_id=54 feature=PCIP51
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 54,
  "feature": "PCIP51",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Health Professions And Related Programs."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 54,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 56. feature_id=55 feature=PCIP52
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 55,
  "feature": "PCIP52",
  "type": "continuous",
  "description": "Percentage of degrees awarded in Business, Management, Marketing, And Related Support Services."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 55,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 57. feature_id=56 feature=PCIP54
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 56,
  "feature": "PCIP54",
  "type": "continuous",
  "description": "Percentage of degrees awarded in History."
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 56,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 58. feature_id=57 feature=PCTPELL
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 57,
  "feature": "PCTPELL",
  "type": "continuous",
  "description": "Percentage of undergraduates who receive a Pell Grant"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 57,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 59. feature_id=58 feature=PFTFAC
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 58,
  "feature": "PFTFAC",
  "type": "continuous",
  "description": "Proportion of faculty that is full-time"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 58,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 60. feature_id=59 feature=PPTUG_EF
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 59,
  "feature": "PPTUG_EF",
  "type": "continuous",
  "description": "Share of undergraduate, degree-/certificate-seeking students who are part-time"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 59,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 61. feature_id=60 feature=PPTUG_EF2
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 60,
  "feature": "PPTUG_EF2",
  "type": "continuous",
  "description": "Share of undergraduate, degree-/certificate-seeking students who are part-time"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 60,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 62. feature_id=61 feature=SATMTMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 61,
  "feature": "SATMTMID",
  "type": "continuous",
  "description": "Midpoint of SAT scores at the institution (math)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 61,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 63. feature_id=62 feature=SATVRMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 62,
  "feature": "SATVRMID",
  "type": "continuous",
  "description": "Midpoint of SAT scores at the institution (critical reading)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 62,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 64. feature_id=63 feature=SATWRMID
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 63,
  "feature": "SATWRMID",
  "type": "continuous",
  "description": "Midpoint of SAT scores at the institution (writing)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 63,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 65. feature_id=64 feature=STABBR
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 64,
  "feature": "STABBR",
  "type": "categorical",
  "description": "State postcode"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 64,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 66. feature_id=65 feature=TUITFTE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 65,
  "feature": "TUITFTE",
  "type": "continuous",
  "description": "Net tuition revenue per full-time equivalent student"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 65,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 67. feature_id=66 feature=TUITIONFEE_IN
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 66,
  "feature": "TUITIONFEE_IN",
  "type": "continuous",
  "description": "In-state tuition and fees"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 66,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 68. feature_id=67 feature=TUITIONFEE_OUT
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 67,
  "feature": "TUITIONFEE_OUT",
  "type": "continuous",
  "description": "Out-of-state tuition and fees"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 67,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 69. feature_id=68 feature=TUITIONFEE_PROG
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 68,
  "feature": "TUITIONFEE_PROG",
  "type": "continuous",
  "description": "Tuition and fees for program-year institutions"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 68,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 70. feature_id=69 feature=UG
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 69,
  "feature": "UG",
  "type": "continuous",
  "description": "Enrollment of all undergraduate students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 69,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 71. feature_id=70 feature=UGDS
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 70,
  "feature": "UGDS",
  "type": "continuous",
  "description": "Enrollment of undergraduate degree-seeking students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 70,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 72. feature_id=71 feature=UGDS_2MOR
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 71,
  "feature": "UGDS_2MOR",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are two or more races"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 71,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 73. feature_id=72 feature=UGDS_AIAN
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 72,
  "feature": "UGDS_AIAN",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are American Indian/Alaska Native"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 72,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 74. feature_id=73 feature=UGDS_AIANOld
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 73,
  "feature": "UGDS_AIANOld",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are American Indian/Alaska Native"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 73,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 75. feature_id=74 feature=UGDS_API
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 74,
  "feature": "UGDS_API",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are Asian/Pacific Islander"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 74,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 76. feature_id=75 feature=UGDS_ASIAN
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 75,
  "feature": "UGDS_ASIAN",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are Asian"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 75,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 77. feature_id=76 feature=UGDS_BLACK
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 76,
  "feature": "UGDS_BLACK",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are black"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 76,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 78. feature_id=77 feature=UGDS_BLACKNH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 77,
  "feature": "UGDS_BLACKNH",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are black non-Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 77,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 79. feature_id=78 feature=UGDS_HISP
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 78,
  "feature": "UGDS_HISP",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 78,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 80. feature_id=79 feature=UGDS_HISPOld
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 79,
  "feature": "UGDS_HISPOld",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 79,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 81. feature_id=80 feature=UGDS_NHPI
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 80,
  "feature": "UGDS_NHPI",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are Native Hawaiian/Pacific Islander"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 80,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 82. feature_id=81 feature=UGDS_NRA
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 81,
  "feature": "UGDS_NRA",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are non-resident aliens"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 81,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 83. feature_id=82 feature=UGDS_UNKN
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 82,
  "feature": "UGDS_UNKN",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students whose race is unknown"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 82,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 84. feature_id=83 feature=UGDS_WHITE
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 83,
  "feature": "UGDS_WHITE",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are white"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 83,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 85. feature_id=84 feature=UGDS_WHITENH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 84,
  "feature": "UGDS_WHITENH",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate degree-seeking students who are white non-Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 84,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 86. feature_id=85 feature=UG_AIANOld
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 85,
  "feature": "UG_AIANOld",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are American Indian/Alaska Native"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 85,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 87. feature_id=86 feature=UG_API
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 86,
  "feature": "UG_API",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are Asian/Pacific Islander"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 86,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 88. feature_id=87 feature=UG_BLACKNH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 87,
  "feature": "UG_BLACKNH",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are black non-Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 87,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 89. feature_id=88 feature=UG_HISPOld
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 88,
  "feature": "UG_HISPOld",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 88,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 90. feature_id=89 feature=UG_NRA
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 89,
  "feature": "UG_NRA",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are non-resident aliens"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 89,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 91. feature_id=90 feature=UG_UNKN
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 90,
  "feature": "UG_UNKN",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students whose race is unknown"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 90,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 92. feature_id=91 feature=UG_WHITENH
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 91,
  "feature": "UG_WHITENH",
  "type": "continuous",
  "description": "Total share of enrollment of undergraduate students who are white non-Hispanic"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 91,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 93. feature_id=92 feature=age_entry
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 92,
  "feature": "age_entry",
  "type": "continuous",
  "description": "Average age of entry, via SSA data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 92,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 94. feature_id=93 feature=age_entry_sq
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 93,
  "feature": "age_entry_sq",
  "type": "continuous",
  "description": "Average of the age of entry squared"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 93,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 95. feature_id=94 feature=agege24
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 94,
  "feature": "agege24",
  "type": "continuous",
  "description": "Percent of students over 23 at entry"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 94,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 96. feature_id=95 feature=dependent
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 95,
  "feature": "dependent",
  "type": "continuous",
  "description": "Share of dependent students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 95,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 97. feature_id=96 feature=faminc
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 96,
  "feature": "faminc",
  "type": "continuous",
  "description": "Average family income"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 96,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 98. feature_id=97 feature=female
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 97,
  "feature": "female",
  "type": "continuous",
  "description": "Share of female students, via SSA data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 97,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 99. feature_id=98 feature=first_gen
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 98,
  "feature": "first_gen",
  "type": "continuous",
  "description": "Share of first-generation students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 98,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 100. feature_id=99 feature=loan_ever
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 99,
  "feature": "loan_ever",
  "type": "continuous",
  "description": "Share of students who received a federal loan while in school"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 99,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 101. feature_id=100 feature=locale2
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 100,
  "feature": "locale2",
  "type": "continuous",
  "description": "Degree of urbanization of institution"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 100,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 102. feature_id=101 feature=main
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 101,
  "feature": "main",
  "type": "categorical",
  "description": "Flag for main campus"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 101,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 103. feature_id=102 feature=married
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 102,
  "feature": "married",
  "type": "continuous",
  "description": "Share of married students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 102,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 104. feature_id=103 feature=md_faminc
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 103,
  "feature": "md_faminc",
  "type": "continuous",
  "description": "Median family income"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 103,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 105. feature_id=104 feature=median_hh_inc
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 104,
  "feature": "median_hh_inc",
  "type": "continuous",
  "description": "Median household income"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 104,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 106. feature_id=105 feature=pct_asian
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 105,
  "feature": "pct_asian",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes that is Asian, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 105,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 107. feature_id=106 feature=pct_ba
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 106,
  "feature": "pct_ba",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes with a bachelor's degree over the age 25, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 106,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 108. feature_id=107 feature=pct_black
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 107,
  "feature": "pct_black",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes that is Black, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 107,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 109. feature_id=108 feature=pct_born_us
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 108,
  "feature": "pct_born_us",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes that was born in the US, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 108,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 110. feature_id=109 feature=pct_grad_prof
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 109,
  "feature": "pct_grad_prof",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes over 25 with a professional degree, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 109,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 111. feature_id=110 feature=pct_hispanic
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 110,
  "feature": "pct_hispanic",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes that is Hispanic, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 110,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 112. feature_id=111 feature=pct_white
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 111,
  "feature": "pct_white",
  "type": "continuous",
  "description": "Percent of the population from students' zip codes that is White, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 111,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 113. feature_id=112 feature=pell_ever
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 112,
  "feature": "pell_ever",
  "type": "continuous",
  "description": "Share of students who received a Pell Grant while in school"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 112,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 114. feature_id=113 feature=poverty_rate
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 113,
  "feature": "poverty_rate",
  "type": "continuous",
  "description": "Poverty rate, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 113,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 115. feature_id=114 feature=region
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 114,
  "feature": "region",
  "type": "categorical",
  "description": "Region (IPEDS)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 114,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 116. feature_id=115 feature=sch_deg
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 115,
  "feature": "sch_deg",
  "type": "continuous",
  "description": "Predominant degree awarded (recoded 0s and 4s)"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 115,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 117. feature_id=116 feature=unemp_rate
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 116,
  "feature": "unemp_rate",
  "type": "continuous",
  "description": "Unemployment rate, via Census data"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 116,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.

## 118. feature_id=117 feature=veteran
You are helping design a feature-importance prior for an out-of-distribution
generalization experiment on the `tableshift` benchmark dataset `college_scorecard`.

Research and use your knowledge of the benchmark and task definition. Prefer
features that should remain semantically predictive across train/test domains,
and penalize features that likely encode domain identifiers, leakage,
demographic shortcuts, or brittle distributional artifacts.

Score this feature by its stable predictive usefulness for this
task, considered marginally and without seeing the downstream data.

Feature card:
{
  "feature_id": 117,
  "feature": "veteran",
  "type": "continuous",
  "description": "Share of veteran students"
}

Return only valid JSON. Do not wrap it in Markdown.
Use exactly this schema:
{
  "task_summary": "one concise sentence about the prediction target",
  "feature_id": 117,
  "score": 0.0,
  "rationale": "short reason"
}

Rules:
- `score` must be a number from 0.0 to 1.0.
- Higher score means stronger expected stable predictive usefulness.
- Penalize sensitive or domain-specific shortcuts, leakage, and brittle artifacts.
- `feature_id` is the authoritative identifier. Do not include feature names in
  the output.
